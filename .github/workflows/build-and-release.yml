# ===================================================================
# Hare Browser Extension CI/CD Pipeline
#
# Automates testing, building, and releasing the Hare browser extension.
#
# Workflow Architecture:
#   1. Test: Runs unit tests to validate code quality
#   2. Build: Compiles extensions for Chrome, Firefox, and Safari in parallel
#   3. Release: Creates a draft GitHub Release with extension artifacts
#
# Triggering Conditions:
#   - On push to `main`:
#     - Executes `test` and `build` jobs
#     - Produces versioned build artifacts
#   - On push of a tag (e.g., `v1.0.0`):
#     - Executes `test`, `build`, and `release` jobs
#     - Creates a formal draft release with all browser extensions
#   - On pull requests to `main`:
#     - Executes `test` and `build` jobs to validate changes
# ===================================================================

name: Build & Release

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ "main" ]

jobs:
  # ===================================================================
  # JOB: test
  #
  # Runs unit tests to ensure code quality before building.
  # ===================================================================
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

  # ===================================================================
  # JOB: build
  #
  # Compiles and packages browser extensions using a matrix strategy.
  # Each browser builds in parallel on its own runner, preventing
  # artifact conflicts and improving performance.
  # ===================================================================
  build:
    name: Build ${{ matrix.browser }} Extension
    runs-on: ubuntu-latest
    needs: test
    strategy:
      fail-fast: false
      matrix:
        browser: [chrome, firefox, safari]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Get version
        id: version
        shell: bash
        run: |
          if [[ $GITHUB_REF == refs/tags/v* ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            VERSION=$(node -p "require('./package.json').version")
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build and zip ${{ matrix.browser }} extension
        run: npx wxt zip -b ${{ matrix.browser }} --mv3
        continue-on-error: ${{ matrix.browser == 'safari' }}

      - name: Prepare artifacts
        id: prepare
        shell: bash
        run: |
          # Find the extension folder (handling -mv2 or -mv3)
          EXT_DIR=$(find .output -maxdepth 1 -type d -name "${{ matrix.browser }}-mv*" | head -n 1)
          echo "ext_dir=$EXT_DIR" >> $GITHUB_OUTPUT
          
          # Find the extension zip (excluding sources)
          ZIP_FILE=$(ls .output/hare-*-${{ matrix.browser }}.zip | grep -v "sources" | head -n 1)
          cp "$ZIP_FILE" "${{ matrix.browser }}-release.zip"
          echo "zip_file=${{ matrix.browser }}-release.zip" >> $GITHUB_OUTPUT

      - name: Upload loadable folder (Manual Test)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.browser }}-extension-v${{ steps.version.outputs.version }}
          path: ${{ steps.prepare.outputs.ext_dir }}/
          if-no-files-found: ${{ matrix.browser == 'safari' && 'ignore' || 'error' }}
          retention-days: 30

      - name: Upload extension zip (Release Asset)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.browser }}-release-v${{ steps.version.outputs.version }}
          path: ${{ steps.prepare.outputs.zip_file }}
          if-no-files-found: ${{ matrix.browser == 'safari' && 'ignore' || 'error' }}
          retention-days: 30

  # ===================================================================
  # JOB: release
  #
  # Creates a draft GitHub Release and attaches browser extension ZIPs.
  # Only executes for version tag pushes.
  # ===================================================================
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          pattern: '*-release-v*'
          merge-multiple: true

      - name: Create Draft Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.zip
          draft: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
